const c = document.querySelector('#security-canvas'), x = c.getContext('2d'); let n = []; function size() { c.width = innerWidth * devicePixelRatio; c.height = innerHeight * devicePixelRatio; x.scale(devicePixelRatio, devicePixelRatio); n = Array.from({ length: 65 }, () => ({ x: Math.random() * innerWidth, y: Math.random() * innerHeight, vx: (Math.random() - .5) * .25, vy: (Math.random() - .5) * .25 })) } function draw() { x.clearRect(0, 0, innerWidth, innerHeight); n.forEach(a => { a.x += a.vx; a.y += a.vy; if (a.x < 0 || a.x > innerWidth) a.vx *= -1; if (a.y < 0 || a.y > innerHeight) a.vy *= -1; n.forEach(b => { let d = Math.hypot(a.x - b.x, a.y - b.y); if (d < 140) { x.strokeStyle = `rgba(109,224,184,${.13 * (1 - d / 140)})`; x.beginPath(); x.moveTo(a.x, a.y); x.lineTo(b.x, b.y); x.stroke() } }); x.fillStyle = '#8bf6cd'; x.shadowBlur = 12; x.shadowColor = '#5de4b3'; x.beginPath(); x.arc(a.x, a.y, 2, 0, 7); x.fill(); x.shadowBlur = 0 }); requestAnimationFrame(draw) } size(); draw(); addEventListener('resize', size);
